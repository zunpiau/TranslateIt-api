{
  "name": "TranslateIt-API",
  "version": "1.0.0",
  "license": "Apache-2.0",
  "dependencies": {},
  "devDependencies": {
    "html-minifier": "^3.5.18",
    "watch": "^1.0.2"
  },
  "scripts": {
    "minify": "html-minifier --minify-css --remove-comments --collapse-whitespace --input-dir src/ --output-dir view/",
    "watch:minify": "watch 'yarn run minify' src/ --wait 0"
  }
}
